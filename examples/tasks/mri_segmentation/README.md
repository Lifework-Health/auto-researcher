# MRI segmentation extension sketch

This directory documents a future plugin shape only. `mri_segmentation` is not
registered or executable in PR 2. See
`docs/task_plugins/MRI_SEGMENTATION_EXAMPLE.md`.
